# WhatsApp Webhook Configuration
WP_VERIFY_TOKEN=your_verify_token_here

# WhatsApp Business API (from Meta Developer Dashboard)
IDENTIFIER_WP_NUMBER=your_phone_number_id
ACCESS_TOKEN_WP_BUSINESS=your_access_token

# Firebase Configuration
FIREBASE_PROJECT_ID=pay-tracker-7a5a6

# Firebase Service Account (base64 encoded JSON)
# Generate from: Firebase Console > Project Settings > Service Accounts > Generate new private key
# Then encode: cat service-account.json | base64
FIREBASE_SERVICE_ACCOUNT=your_base64_encoded_service_account_json
